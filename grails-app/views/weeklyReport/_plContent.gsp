<g:each in="${plan}" status="i" var="p">
    <div>
        <span style="width: 70px;text-align: left; color: #0000ff;display: inline-block">${p[0]}</span>
        <span>${p[1]}</span>
    </div>
</g:each>